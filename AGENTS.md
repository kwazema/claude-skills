# AGENTS.md

Este repositorio usa `CLAUDE.md` como fuente principal de instrucciones.

@CLAUDE.md

Si el agente no soporta imports con `@`, debe leer `CLAUDE.md` manualmente antes de actuar.

Si hay conflicto entre este archivo y `CLAUDE.md`, manda `CLAUDE.md`.

## Adaptacion para Codex

Las skills que en Claude Code se invocan con `/nombre-skill`, en Codex se invocan con `$nombre-skill`.
